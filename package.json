{
	"name": "api.kernvalley.us",
	"version": "1.0.0",
	"author": "Chris Zuber <shgysk8zer0@gmail.com>",
	"license": "MIT",
	"description": "An API written in PHP & MySQL",
	"keywords": [
		"PHP",
		"MySQL",
		"API"
	],
	"config": {
		"host": "localhost",
		"port": 8081
	},
	"private": true,
	"scripts": {
		"preinstall": "git submodule update --init --recursive",
		"postinstall": "if [ -f './composer.json' ]; then composer install; fi",
		"start": "php -S ${npm_package_config_host}:${npm_package_config_port}",
		"test": "php lint.php"
	}
}
